using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CompileErrorSimulator : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        wagabagobo
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
